# =====================================================================
# CSO Capstone — pinned dependencies
#
# Stack: LangChain + LangGraph + LangSmith + Streamlit
# =====================================================================

# ── Core LLM stack ────────────────────────────────────────────────────
google-genai>=0.8.0,<2.0.0
langsmith>=0.1.140,<0.4.0
langgraph>=0.2.50,<0.6.0
langgraph-checkpoint>=2.0.0,<3.0.0
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-google-genai>=2.0.0,<3.0.0
langchain-community>=0.3.0,<0.4.0

# ── Multi-provider LLM support ────────────────────────────────────────
langchain-openai>=0.2.0,<0.4.0
langchain-anthropic>=0.2.0,<0.4.0
langchain-groq>=0.2.0,<0.4.0
langchain-ollama>=0.2.0,<0.4.0

# ── Data fetch — runtime, no committed CSV ────────────────────────────
kagglehub>=0.3.0,<0.4.0
pandas>=2.0.0,<3.0.0
numpy>=1.26.0,<3.0.0

# ── ML — fraud model ──────────────────────────────────────────────────
xgboost>=2.1.0,<4.0.0
scikit-learn>=1.5.0,<2.0.0
imbalanced-learn>=0.12.0,<0.15.0       # SMOTE for the fraud class imbalance
joblib>=1.4.0,<2.0.0                   # model persistence

# ── Dashboard ─────────────────────────────────────────────────────────
streamlit>=1.36.0,<2.0.0
plotly>=5.20.0,<7.0.0

# ── Testing ───────────────────────────────────────────────────────────
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<2.0.0
